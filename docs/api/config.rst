======
Config
======

.. currentmodule:: beemo.settings

The :class:`BuildConfig` class contains the configuration for a Beemo site build. It is available as
the ``config`` attribute of the :class:`TheScribe` object passed into templates, so template authors
can use its attributes to display content.

.. autoclass:: BuildConfig
    :members:
    :show-inheritance: